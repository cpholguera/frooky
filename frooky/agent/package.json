{
  "name": "frooky-agent",
  "version": "0.0.1",
  "description": "Frooky frida agent.",
  "private": true,
  "type": "module",
  "scripts": {
    "build:prod:android": "node build.js -p android -c --type-check none",
    "build:prod:ios": "node build.js -p ios -c --type-check none",

    "build:dev:android": "node build.js -p android --type-check none",
    "build:dev:ios": "node build.js -p ios --type-check none",

    "build:watch:android": "node build.js -p android -t frida -w --type-check none",
    "build:watch:ios": "node build.js -p ios -w -t frida --type-check none",

    "build:test:android": "mkdir -p dist && frida-compile --type-check none tests/android/agent-runner.ts -o dist/agent-test-android.js",
    "build:test:ios": "mkdir -p dist && frida-compile --type-check none tests/ios/agent-runner.ts -o dist/agent-test-ios.js",

    "test:android": "npm run build:test:android && node tests/run-tests.js -p dist/agent-test-android.js -usb",
    "test:ios:usb": "npm run build:test:ios && node tests/run-tests.js -p dist/agent-test-ios.js -usb",
    "test:ios:local": "npm run build:test:ios && tests/run-tests.js -p dist/agent-test-ios.js"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.10",
    "@types/frida-gum": "^19.0.1",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^24.10.9",
    "js-yaml": "^4.1.1",
    "chokidar": "^5.0.0",
    "frida-compile": "^19.0.4",
    "frida-java-bridge": "^7.0.11",
    "frida-objc-bridge": "^8.0.5",
    "frida-swift-bridge": "^3.0.2",
    "minimist": "^1.2.8"
  },
  "overrides": {
    "@isaacs/brace-expansion": "^5.0.1"
  }
}
