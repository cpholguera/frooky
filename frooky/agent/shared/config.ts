export const DECODER_MAX_RECURSION = 10;

// maximal amount of elements retrieved on an iterable object
export const DECODER_MAX_ELEMENTS = 100;

export const DEFAULT_STACK_TRACE_LIMIT = 10;

// specifies the interval between cached events are send back to the host
export const SEND_INTERVAL = 100;
